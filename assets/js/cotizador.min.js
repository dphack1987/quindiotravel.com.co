const DESTINOS_PRECIOS = { 'valle-cocora': 85000, 'salento': 45000, 'filandia': 40000, 'panaca': 65000, 'recuca': 55000, 'termales': 75000, 'mariposario': 35000, 'cafe-tour': 50000 }; function runWhenReady(callback) { if (document.readyState === 'complete' || document.readyState === 'interactive') { callback(); } else { document.addEventListener('DOMContentLoaded', callback, { once: true }); } } window.QUINDIO_TRAVEL_DATA_LOADED = fetch('docs/data/tarifas.json') .then(response => response.json()) .then(data => { window.QUINDIO_TRAVEL_DATA = data; runWhenReady(actualizarUI);